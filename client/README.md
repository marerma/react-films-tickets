Clone the repository, go to client directory and make a crosscheck.

```
git clone
cd client
git checkout develop
npm install
npm run server (runs server with api)
npm run dev (runs project in dev mode)